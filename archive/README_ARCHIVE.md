# Archive

This folder holds legacy code snapshots and notes from earlier architecture versions.

- `legacy` folder may contain previous monolithic scripts.
- Use this for rollback or comparison when major refactor occurs.
